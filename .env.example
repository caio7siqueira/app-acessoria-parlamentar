# 📋 App Assessoria Parlamentar - Variáveis de Ambiente
# Copie este arquivo para .env.local e preencha com seus valores reais

# ===============================================
# 🔑 SUPABASE (OBRIGATÓRIO)
# ===============================================
# URL do projeto Supabase (encontrado em: Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Chave pública anônima (encontrada em: Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Chave de service role (OPCIONAL - para operações administrativas)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ===============================================
# 🔐 NEXTAUTH (OBRIGATÓRIO)
# ===============================================
# URL da aplicação (localhost para desenvolvimento)
NEXTAUTH_URL=http://localhost:3000

# Chave secreta para criptografia (gere com: openssl rand -base64 32)
NEXTAUTH_SECRET=your-secret-here-use-openssl-rand-base64-32

# ===============================================
# 📱 PUSH NOTIFICATIONS (OPCIONAL)
# ===============================================
# Chaves VAPID para notificações push (gere com: node generateVapid.js)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key

# ===============================================
# 🚀 DEPLOY (APENAS PARA PRODUÇÃO)
# ===============================================
# URL do Vercel (preenchido automaticamente no deploy)
VERCEL_URL=your-app-url.vercel.app

# Ambiente de execução
NODE_ENV=development

# ===============================================
# 📝 INSTRUÇÕES:
# ===============================================
# 1. Copie este arquivo: cp .env.example .env.local
# 2. Preencha as variáveis obrigatórias (Supabase + NextAuth)
# 3. As variáveis opcionais podem ser preenchidas depois
# 4. NUNCA commite o arquivo .env.local no git